package game.in_game;

import game.BasedWindow;

import javax.swing.*;

public class Debug extends JFrame {
    BasedWindow basedWindow;
    Debug(BasedWindow basedWindow) {
        this.basedWindow = basedWindow;
    }
}
